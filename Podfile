# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
# ignore all warnings from all dependencies
inhibit_all_warnings!

target 'TempoMobileTask' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire', '~> 5'
  pod 'CodableCache'
  pod 'RxSwift', '~> 5'
  pod 'RxCocoa', '~> 5'
  pod 'SwiftLint', '~> 0.27'
  pod 'SDWebImage', '~> 5.0'
  # Pods for TempoMobileTask

  target 'TempoMobileTaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TempoMobileTaskUITests' do
    # Pods for testing
  end

end
